import 'dart:ui';

class AppColors {
  AppColors._();

  static const calendarBackgroundColor =  Color.fromRGBO(28, 28, 30, 1);
  static const calendarWeekdayColor =  Color.fromRGBO(99, 99, 102, 1);
  static const zeroOpacityColor = Color.fromRGBO(0, 0, 0, 0);
}